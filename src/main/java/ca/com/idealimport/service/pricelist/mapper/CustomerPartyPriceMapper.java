package ca.com.idealimport.service.pricelist.mapper;

import org.mapstruct.Mapper;

@Mapper
public interface CustomerPartyPriceMapper {
    /*
    CustomerPartyPriceMapper INSTANCE = Mappers.getMapper(CustomerPartyPriceMapper.class);

    @Mapping(source = "itemPrices", target = "itemPrices")
    @Mapping(source = "customer", target = "customer")
    @Mapping(source = "party", target = "party")
    CustomerParty toCustomerParty(CustomerPriceDto customerPriceDTO, Customer customer, Party party);

    @Mapping(source = "itemId", target = "itemPriceId")
    @Mapping(source = "amount", target = "price")
    ItemPrice toItemPrice(PriceItemDto itemPriceDTO);

    @Mapping(source = "itemPrices", target = "itemPrices")
    @Mapping(source = "customer", target = "customer")
    @Mapping(source = "party", target = "party")
    CustomerPriceResponse toCustomerPriceResponse(CustomerParty customerParty);

    @Mapping(source = "customerId", target = "key")
    @Mapping(source = "customerName", target = "value")
    DropDownDto toDropDownDTO(Customer customer);

    @Mapping(source = "partyId", target = "key")
    @Mapping(source = "fullName", target = "value")
    DropDownDto toDropDownDTO(Party party);
*/
}

